'use client'

import styled from "styled-components";
import {DARK_DRAY, GRAY} from "@/constants";

export const InputContainer = styled.div`
  min-height: 50px;
  border-radius: 5px;
  border: 1px solid ${DARK_DRAY};
  background-color: ${GRAY};
  width: 100%;
`